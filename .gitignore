# Auto-generated base64 data URL shims (~100MB+).
# Regenerate locally with: node generate-manifest.js
# Not needed on Netlify — the https:// export uses fetch→blob directly.
Assets/data/
assets/data/

# OS cruft
.DS_Store
Thumbs.db

# Local Netlify folder
.netlify

# Claude Code local config / worktrees
.claude/

# Node — dependencies installed by npm/yarn. package.json + package-lock.json
# are tracked; everything inside node_modules/ is not.
node_modules/
